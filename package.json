{
  "name": "telemetry-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Telemetry resilient communications control plane",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
